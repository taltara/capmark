谢谢，已经提了：leenkcool/Blue-Whale-Harness#69

两个仓库：**taltara/capmark**（`sandbox`）和 **taltara/mddl-harness**（`ui`）。

只改了 `repos.json`，没有动 `README.md` —— 它由 `catalog/build-readme.mjs` 生成，
手改会和你下次重新生成冲突。序列化沿用了原有格式，所以 diff 只有新增的 32 行，
没有整文件重排。

顺带一个可能对这个库有用的数据。我用 `capmark infer` 扫了 16 个已发布的安全类插件：
10 个直接读取文件系统，2 个访问网络，**没有一个**在任何机器可读的地方声明这一点。

这不是指责任何插件——供应链扫描器读文件本来就是它该做的事，样本里的每个插件看起来
都很合理。问题在于读者只能凭信任。完整数据和可复现的脚本（只从 npm 拉包做静态读取，
不执行任何代码）：
<https://github.com/taltara/capmark/blob/main/docs/audit-2026-08.md>

如果对 `Blue-Whale-Harness` 有用，`capmark infer <包目录>` 可以批量给每个已收录插件
生成"它能接触到什么"的草稿，作为目录的一个补充字段。这方面我很乐意帮忙。

---

Thanks — submitted: leenkcool/Blue-Whale-Harness#69

Two repos: **taltara/capmark** (`sandbox`) and **taltara/mddl-harness** (`ui`).

I edited only `repos.json`. `README.md` is generated by
`catalog/build-readme.mjs`, so hand-editing it would conflict with your next
regeneration — the diff is 32 added lines with no reformatting.

One number that may be useful for the catalogue itself: running `capmark infer`
over sixteen published security plugins, ten read the filesystem, two reach the
network, and none declare it anywhere a machine can check. Not a complaint about
any of them — a scanner that reads files is doing its job — but the reader has
to take it on faith. If a "what can this reach" field would be useful for
`Blue-Whale-Harness`, the inference is static and scriptable and I am happy to
help wire it in.
